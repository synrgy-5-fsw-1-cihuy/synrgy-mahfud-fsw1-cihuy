/**
 Disini kamu akan membuat sebuah fungsi yang berguna untuk mensortir daftar mobil,
 dimana setelah fungsi ini dijalankan maka daftar mobil yang telah diurutkan dari 
 umur mobil paling tua akan menjadi hasil dari fungsi tersebut.
**/

function sortCarByYearAscendingly(cars) {
    // Tulis code-mu disini
    const result = [];

    return result; 
}

sortCarByYearAscendingly(cars);